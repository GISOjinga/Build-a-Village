"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.lineModifier = lineModifier;
var modifier = "%l";
function lineModifier(toFormat, source, node) {
    if (!toFormat.includes(modifier))
        return toFormat;
    var line = source.getLineAndCharacterOfPosition(node.getStart()).line + 1;
    return toFormat.replace(modifier, line.toString());
}
