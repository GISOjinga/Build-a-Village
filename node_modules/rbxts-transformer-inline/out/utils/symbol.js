"use strict";
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.SymbolUtils = void 0;
var typescript_1 = __importDefault(require("typescript"));
var symbols_1 = require("../symbols");
var SymbolUtils = /** @class */ (function () {
    function SymbolUtils(context) {
        this.context = context;
        this.symbols = new symbols_1.Symbols();
    }
    SymbolUtils.prototype.transformSymbol = function (node) {
        var transformer = this.symbols.getTransformer(node.text);
        if (!transformer)
            return node;
        return transformer(this.context, node);
    };
    SymbolUtils.prototype.isCorrectImport = function (symbol, correctImport, isFromType) {
        var e_1, _a;
        var _b;
        if (correctImport === void 0) { correctImport = "rbxts-transformer-inline"; }
        if (isFromType === void 0) { isFromType = false; }
        if (!symbol.declarations)
            return false;
        try {
            for (var _c = __values(symbol.declarations), _d = _c.next(); !_d.done; _d = _c.next()) {
                var declaration = _d.value;
                if (!isFromType) {
                    var isImportSpecifier = typescript_1.default.isImportSpecifier(declaration);
                    if (!isImportSpecifier && !typescript_1.default.isImportDeclaration(declaration.parent))
                        continue;
                    var importDeclaration = isImportSpecifier ? declaration.parent.parent.parent : declaration.parent;
                    if (!typescript_1.default.isImportDeclaration(importDeclaration))
                        continue;
                    var moduleSpecifier = importDeclaration.moduleSpecifier;
                    if (!typescript_1.default.isStringLiteral(moduleSpecifier))
                        continue;
                    if (moduleSpecifier.text !== correctImport)
                        continue;
                    return true;
                }
                else {
                    var fileName = declaration.getSourceFile().fileName;
                    var match = (_b = fileName.match(/node_modules\/(@[^\/]+\/[^\/]+|[^\/]+)/)) !== null && _b !== void 0 ? _b : [undefined, undefined];
                    return match[1] === correctImport;
                }
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (_d && !_d.done && (_a = _c.return)) _a.call(_c);
            }
            finally { if (e_1) throw e_1.error; }
        }
        return false;
    };
    return SymbolUtils;
}());
exports.SymbolUtils = SymbolUtils;
