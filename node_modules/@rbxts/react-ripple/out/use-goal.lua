--!nonstrict
--!optimize 2
-- Compiled with roblox-ts v2.3.0-dev-576ad98
local TS = _G[script]
local GoalType = TS.import(script, script.Parent, "goal-type").default
local Ripple = TS.import(script, TS.getModule(script, "@rbxts", "ripple").src)
local getBinding = TS.import(script, script.Parent, "get-binding").default
local useMotion = TS.import(script, script.Parent, "use-motion").default
local GOALS_TO_FUNCTIONS = {
	[GoalType.Immediate] = Ripple.immediate,
	[GoalType.Linear] = Ripple.linear,
	[GoalType.Spring] = Ripple.spring,
	[GoalType.Tween] = Ripple.tween,
}
--[[
	*
	 * Uses a goal. I don't really know what this is for.
	 * @param goal
	 * @param value
	 * @param motionOptions
	 
]]
local function useGoal(goal, value, motionOptions, goalOptions)
	local motion = useMotion(value, motionOptions)
	motion:to(GOALS_TO_FUNCTIONS[goal](value, goalOptions))
	return getBinding(motion), motion
end
return {
	default = useGoal,
}
