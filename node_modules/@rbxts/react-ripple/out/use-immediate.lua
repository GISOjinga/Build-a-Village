--!nonstrict
--!optimize 2
-- Compiled with roblox-ts v2.3.0-dev-576ad98
local TS = _G[script]
local GoalType = TS.import(script, script.Parent, "goal-type").default
local useGoal = TS.import(script, script.Parent, "use-goal").default
--[[
	*
	 * Syntax sugar for creating an immediate motion.
	 * @param value
	 * @param motionOptions
	 * @returns
	 
]]
local function useImmediate(value, motionOptions)
	return useGoal(GoalType.Immediate, value, motionOptions)
end
return {
	default = useImmediate,
}
