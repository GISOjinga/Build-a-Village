import type { Motion, MotionGoal } from "@rbxts/ripple";
/**
 * The simplest way to use a motion and binding.
 *
 * @example
 * ```ts
 * const [binding, motion] = useMotionBinding(useMotion<number>(0));
 * useEffect(() => {
 * 	motion.spring(1);
 * }, []);
 * ```
 *
 * @param motion
 * @returns
 */
export declare function useMotionBinding<T extends MotionGoal>(motion: Motion<T>): LuaTuple<[import("@rbxts/react").Binding<T>, Motion<T>]>;
