--!nonstrict
--!optimize 2
-- Compiled with roblox-ts v2.3.0-dev-576ad98
local TS = _G[script]
local GoalType = TS.import(script, script.Parent, "goal-type").default
local useGoal = TS.import(script, script.Parent, "use-goal").default
--[[
	*
	 * Syntax sugar for creating a linear motion.
	 * @param value
	 * @param motionOptions
	 * @param linearOptions
	 * @returns
	 
]]
local function useLinear(value, motionOptions, linearOptions)
	return useGoal(GoalType.Linear, value, motionOptions, linearOptions)
end
return {
	default = useLinear,
}
