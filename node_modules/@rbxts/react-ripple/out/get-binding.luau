--!nocheck
--!nolint
--!optimize 2
-- Compiled with roblox-ts v3.0.0
local TS = _G[script]
local Symbol = TS.import(script, script.Parent, "symbol")
local React = TS.import(script, TS.getModule(script, "@rbxts", "react"))
local AssignedBinding = Symbol("AssignedBinding")
--[[
	*
	 * Gets a binding from a motion. Creates it if it doesn't exist.
	 * @param motion
	 * @returns
	 
]]
local function getBinding(motion)
	if motion[AssignedBinding] ~= nil then
		return motion[AssignedBinding]
	end
	local binding, setBinding = React.createBinding(motion:get())
	motion:onStep(setBinding)
	motion[AssignedBinding] = binding
	return binding
end
return {
	getBinding = getBinding,
}
