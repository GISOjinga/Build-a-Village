--!nonstrict
--!optimize 2
-- Compiled with roblox-ts v2.3.0-dev-576ad98
local TS = _G[script]
local GoalType = TS.import(script, script.Parent, "goal-type").default
local useGoal = TS.import(script, script.Parent, "use-goal").default
--[[
	*
	 * Syntax sugar for creating a spring motion.
	 * @param value
	 * @param motionOptions
	 * @param springOptions
	 * @returns
	 
]]
local function useSpring(value, motionOptions, springOptions)
	return useGoal(GoalType.Spring, value, motionOptions, springOptions)
end
return {
	default = useSpring,
}
