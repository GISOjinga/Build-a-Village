-- Compiled with roblox-ts v3.0.0
local TS = _G[script]
local source = TS.import(script, TS.getModule(script, "@rbxts", "vide").src).source
local DEFAULT_INTERFACE_OPTIONS = TS.import(script, script.Parent, "constants", "options").DEFAULT_INTERFACE_OPTIONS
local visible = source(false)
local options = source(DEFAULT_INTERFACE_OPTIONS)
local mouseOverInterface = source(false)
local currentCommandPath = source(nil)
local commandArgIndex = source(nil)
local terminalArgIndex = source(nil)
local currentSuggestion = source(nil)
local terminalText = source("")
local terminalTextValid = source(false)
local currentTextPart = source(nil)
return {
	visible = visible,
	options = options,
	mouseOverInterface = mouseOverInterface,
	currentCommandPath = currentCommandPath,
	commandArgIndex = commandArgIndex,
	terminalArgIndex = terminalArgIndex,
	currentSuggestion = currentSuggestion,
	terminalText = terminalText,
	terminalTextValid = terminalTextValid,
	currentTextPart = currentTextPart,
}
