-- Compiled with roblox-ts v3.0.0
local TS = _G[script]
local source = TS.import(script, TS.getModule(script, "@rbxts", "vide").src).source
local clientState = source()
local function useClient(client)
	local value = clientState()
	if value ~= nil then
		return value
	end
	local _arg0 = client ~= nil
	assert(_arg0, "No client has been set.")
	clientState(client)
	return client
end
return {
	useClient = useClient,
}
