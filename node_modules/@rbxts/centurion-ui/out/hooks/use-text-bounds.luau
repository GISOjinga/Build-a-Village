-- Compiled with roblox-ts v3.0.0
local TS = _G[script]
local TextService = TS.import(script, TS.getModule(script, "@rbxts", "services")).TextService
local _vide = TS.import(script, TS.getModule(script, "@rbxts", "vide").src)
local cleanup = _vide.cleanup
local derive = _vide.derive
local read = _vide.read
local function useTextBounds(_param)
	local text = _param.text
	local font = _param.font
	local size = _param.size
	local width = _param.width
	local params = Instance.new("GetTextBoundsParams")
	params.RichText = true
	cleanup(params)
	return derive(function()
		local textValue = read(text)
		if textValue == nil then
			return Vector2.zero
		end
		params.Text = textValue
		local fontValue = read(font)
		local sizeValue = read(size)
		local widthValue = read(width)
		if fontValue ~= nil then
			params.Font = fontValue
		end
		if sizeValue ~= nil then
			params.Size = sizeValue
		end
		if widthValue ~= nil then
			params.Width = widthValue
		end
		return TextService:GetTextBoundsAsync(params)
	end)
end
return {
	useTextBounds = useTextBounds,
}
