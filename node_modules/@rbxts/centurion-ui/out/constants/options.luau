-- Compiled with roblox-ts v3.0.0
local TS = _G[script]
local GuiService = TS.import(script, TS.getModule(script, "@rbxts", "services")).GuiService
local DefaultPalette = TS.import(script, script.Parent.Parent, "palette").DefaultPalette
local DEFAULT_INTERFACE_OPTIONS = {
	anchor = Vector2.new(),
	position = function(px)
		return UDim2.fromOffset(px(16), px(8) + (GuiService:GetGuiInset()).Y)
	end,
	size = function(px)
		return UDim2.new(0, px(1024), 1, 0)
	end,
	displayOrder = 1000,
	backgroundTransparency = 0.2,
	hideOnLostFocus = true,
	activationKeys = { Enum.KeyCode.F2 },
	font = {
		regular = Font.new("rbxassetid://16658221428"),
		medium = Font.new("rbxassetid://16658221428", Enum.FontWeight.Medium),
		bold = Font.new("rbxassetid://16658221428", Enum.FontWeight.Bold),
	},
	palette = DefaultPalette.mocha,
	autoLocalize = false,
}
return {
	DEFAULT_INTERFACE_OPTIONS = DEFAULT_INTERFACE_OPTIONS,
}
