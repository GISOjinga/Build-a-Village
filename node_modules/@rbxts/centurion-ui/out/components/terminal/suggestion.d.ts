import { ArgumentOptions, BaseRegistry, ListArgumentType, RegistryPath, SingleArgumentType } from "@rbxts/centurion";
import { ReadonlyDeep } from "@rbxts/centurion/out/shared/util/data";
import { CommandSuggestion } from "../../types";
export interface SingleArgument {
    kind: "single";
    options: ReadonlyDeep<ArgumentOptions>;
    type: SingleArgumentType<unknown>;
    input?: string;
}
export interface ListArgument {
    kind: "list";
    options: ReadonlyDeep<ArgumentOptions>;
    type: ListArgumentType<unknown>;
    input: string[];
}
export type Argument = SingleArgument | ListArgument;
export declare function getArgumentSuggestion(arg: Argument, textPart?: string): {
    type: "argument";
    title: string;
    others: string[];
    description: string;
    dataType: string;
    optional: boolean;
    error: string | undefined;
};
export declare function getCommandSuggestion(registry: BaseRegistry, parentPath?: RegistryPath, text?: string): CommandSuggestion | undefined;
