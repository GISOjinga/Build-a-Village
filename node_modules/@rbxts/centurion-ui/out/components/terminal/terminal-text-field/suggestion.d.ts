import { BaseRegistry } from "@rbxts/centurion";
import { Suggestion } from "../../../types";
export declare function completeCommand(registry: BaseRegistry, text: string, suggestion: string): string;
export declare function completeArgument(text: string, suggestion: string, commandArgCount: number): string;
export declare function getSuggestedText(registry: BaseRegistry, text: string, suggestion?: Suggestion): string;
