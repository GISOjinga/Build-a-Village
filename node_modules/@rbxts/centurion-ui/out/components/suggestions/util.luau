-- Compiled with roblox-ts v3.0.0
local function highlightMatching(color, text, terminalText)
	if text == nil then
		return ""
	end
	if terminalText == nil then
		return text
	end
	local _text = text
	local _arg1 = #terminalText
	local subText = string.sub(_text, 0, _arg1)
	if string.lower(terminalText) ~= string.lower(subText) then
		return text
	end
	local _text_1 = text
	local _arg0 = #terminalText + 1
	local unhighlightedText = string.sub(_text_1, _arg0)
	return `<font color="#{color:ToHex()}">{subText}</font>{unhighlightedText}`
end
return {
	highlightMatching = highlightMatching,
}
