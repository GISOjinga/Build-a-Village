-- Compiled with roblox-ts v3.0.0
local TS = _G[script]
local Vide = TS.import(script, TS.getModule(script, "@rbxts", "vide").src)
local IS_EDIT = TS.import(script, script.Parent.Parent.Parent, "constants", "util").IS_EDIT
local Group = TS.import(script, script.Parent, "group").Group
local function Layer(_param)
	local name = _param.name
	local visible = _param.visible
	local displayOrder = _param.displayOrder
	local children = _param.children
	return if IS_EDIT then (Vide.jsx(Group, {
		name = name,
		visible = visible,
		zIndex = displayOrder,
	}, children)) else (Vide.jsx("screengui", {
		Enabled = visible,
		ResetOnSpawn = false,
		DisplayOrder = displayOrder,
		IgnoreGuiInset = true,
		ZIndexBehavior = "Sibling",
		Name = name,
	}, children))
end
return {
	Layer = Layer,
}
