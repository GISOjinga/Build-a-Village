-- Compiled with roblox-ts v3.0.0
local TS = _G[script]
local _vide = TS.import(script, TS.getModule(script, "@rbxts", "vide").src)
local Vide = _vide
local read = _vide.read
local useAtom = TS.import(script, script.Parent.Parent.Parent, "hooks", "use-atom").useAtom
local interfaceOptions = TS.import(script, script.Parent.Parent.Parent, "store").interfaceOptions
local function TextField(props)
	local options = useAtom(interfaceOptions)
	local _attributes = {
		PlaceholderText = props.placeholderText,
		PlaceholderColor3 = props.placeholderColor,
		ClearTextOnFocus = props.clearTextOnFocus,
		MultiLine = props.multiLine,
		TextEditable = props.textEditable,
		Font = Enum.Font.Unknown,
		FontFace = function()
			return read(props.font) or options().font.regular
		end,
		Text = props.text,
		TextColor3 = props.textColor,
		TextSize = props.textSize,
		TextTransparency = props.textTransparency,
		TextTruncate = props.textTruncate,
		TextWrapped = props.textWrapped,
		TextXAlignment = props.textXAlignment,
		TextYAlignment = props.textYAlignment,
		TextScaled = props.textScaled,
		RichText = props.richText,
		AutomaticSize = props.textAutoResize,
		AutoLocalize = function()
			return options().autoLocalize
		end,
		Size = props.size,
		Position = props.position,
		AnchorPoint = props.anchor,
		BackgroundColor3 = props.backgroundColor,
	}
	local _condition = props.backgroundTransparency
	if _condition == nil then
		_condition = 1
	end
	_attributes.BackgroundTransparency = _condition
	_attributes.ClipsDescendants = props.clipsDescendants
	_attributes.Visible = props.visible
	_attributes.ZIndex = props.zIndex
	_attributes.LayoutOrder = props.layoutOrder
	_attributes.BorderSizePixel = 0
	_attributes.action = props.action
	local _attribute = props.native
	if _attribute then
		for _k, _v in _attribute do
			_attributes[_k] = _v
		end
	end
	return Vide.jsx("textbox", _attributes, props.cornerRadius and Vide.jsx("uicorner", {
		CornerRadius = props.cornerRadius,
	}), props.children)
end
return {
	TextField = TextField,
}
