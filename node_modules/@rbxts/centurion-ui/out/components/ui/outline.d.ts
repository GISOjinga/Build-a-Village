import Vide, { Derivable } from "@rbxts/vide";
interface OutlineProps {
    outlineTransparency?: Derivable<number>;
    innerColor?: Derivable<Color3>;
    outerColor?: Derivable<Color3>;
    innerTransparency?: Derivable<number>;
    outerTransparency?: Derivable<number>;
    innerThickness?: Derivable<number>;
    outerThickness?: Derivable<number>;
    cornerRadius?: Derivable<UDim>;
    children?: Vide.Node;
}
export declare function blend(...transparencies: number[]): number;
export declare function Outline({ outlineTransparency, innerColor, outerColor, innerTransparency, outerTransparency, innerThickness, outerThickness, cornerRadius, children, }: OutlineProps): Vide.Node;
export {};
