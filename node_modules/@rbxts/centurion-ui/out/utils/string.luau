-- Compiled with roblox-ts v3.0.0
local START_QUOTE_PATTERN = `^(['"])`
local END_QUOTE_PATTERN = `(['"])%s*$`
local function containsSpace(text)
	return not (#{ string.match(text, "%s") } == 0)
end
local function stripQuotes(text)
	return (string.gsub((string.gsub(text, START_QUOTE_PATTERN, "")), END_QUOTE_PATTERN, ""))
end
local function getQuoteChar(text)
	return (string.match(text, START_QUOTE_PATTERN))
end
local function isQuoteStarted(text)
	return (string.match(text, START_QUOTE_PATTERN)) ~= nil
end
local function isQuoteEnded(text)
	return (string.match(text, END_QUOTE_PATTERN)) ~= nil
end
return {
	containsSpace = containsSpace,
	stripQuotes = stripQuotes,
	getQuoteChar = getQuoteChar,
	isQuoteStarted = isQuoteStarted,
	isQuoteEnded = isQuoteEnded,
}
