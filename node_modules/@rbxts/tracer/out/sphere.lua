-- Compiled with roblox-ts v2.3.0
local TS = _G[script]
local Workspace = TS.import(script, TS.getModule(script, "@rbxts", "services")).Workspace
local Trace = TS.import(script, script.Parent, "trace").Trace
local Sphere
do
	local super = Trace
	Sphere = setmetatable({}, {
		__tostring = function()
			return "Sphere"
		end,
		__index = super,
	})
	Sphere.__index = Sphere
	function Sphere.new(...)
		local self = setmetatable({}, Sphere)
		return self:constructor(...) or self
	end
	function Sphere:constructor(_radius, startPos, direction)
		super.constructor(self, startPos, direction)
		self._radius = _radius
	end
	function Sphere:_raycastFunc()
		return Workspace:Spherecast(self.startPos, self._radius, self.direction, self._raycastParams)
	end
end
return {
	Sphere = Sphere,
}
