-- Compiled with roblox-ts v2.3.0
local TS = _G[script]
local Workspace = TS.import(script, TS.getModule(script, "@rbxts", "services")).Workspace
local Trace = TS.import(script, script.Parent, "trace").Trace
local Box
do
	local super = Trace
	Box = setmetatable({}, {
		__tostring = function()
			return "Box"
		end,
		__index = super,
	})
	Box.__index = Box
	function Box.new(...)
		local self = setmetatable({}, Box)
		return self:constructor(...) or self
	end
	function Box:constructor(_size, startPos, endPos)
		super.constructor(self, startPos, endPos)
		self._size = _size
	end
	function Box:setRotation(cframe)
		self._rotation = cframe
		return self
	end
	function Box:_raycastFunc()
		local _fn = Workspace
		local _cFrame = CFrame.new(self.startPos)
		local _arg0 = self._rotation or CFrame.identity
		return _fn:Blockcast(_cFrame * _arg0, self._size, self.direction, self._raycastParams)
	end
end
return {
	Box = Box,
}
