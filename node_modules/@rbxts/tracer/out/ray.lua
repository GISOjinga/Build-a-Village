-- Compiled with roblox-ts v2.3.0
local TS = _G[script]
local Workspace = TS.import(script, TS.getModule(script, "@rbxts", "services")).Workspace
local Trace = TS.import(script, script.Parent, "trace").Trace
local Ray
do
	local super = Trace
	Ray = setmetatable({}, {
		__tostring = function()
			return "Ray"
		end,
		__index = super,
	})
	Ray.__index = Ray
	function Ray.new(...)
		local self = setmetatable({}, Ray)
		return self:constructor(...) or self
	end
	function Ray:constructor(...)
		super.constructor(self, ...)
	end
	function Ray:_raycastFunc()
		return Workspace:Raycast(self.startPos, self.direction, self._raycastParams)
	end
end
return {
	Ray = Ray,
}
