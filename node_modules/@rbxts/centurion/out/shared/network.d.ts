import { CommandContextData, CommandOptions, GroupOptions } from "./types";
export interface SyncData {
    commands: Map<string, CommandOptions>;
    groups: Map<string, GroupOptions>;
}
export declare function getRemotes(): {
    syncStart: RemoteEvent<() => void>;
    syncDispatch: RemoteEvent<(data: SyncData) => void>;
    execute: RemoteFunction<(path: string, args: string[]) => CommandContextData>;
};
