-- Compiled with roblox-ts v3.0.0
local TS = _G[script]
local Teams = TS.import(script, TS.getModule(script, "@rbxts", "services")).Teams
local CenturionType = TS.import(script, script.Parent).CenturionType
local _core = TS.import(script, script.Parent.Parent.Parent, "core")
local TransformResult = _core.TransformResult
local TypeBuilder = _core.TypeBuilder
local teamType = TypeBuilder:create(CenturionType.Team):transform(function(text)
	local team = Teams:FindFirstChild(text)
	if team == nil or not (team.ClassName == "Team") then
		return TransformResult.err(`Team not found: {text}`)
	end
	return TransformResult.ok(team)
end):suggestions(function()
	local _exp = Teams:GetChildren()
	-- ▼ ReadonlyArray.map ▼
	local _newValue = table.create(#_exp)
	local _callback = function(team)
		return team.Name
	end
	for _k, _v in _exp do
		_newValue[_k] = _callback(_v, _k - 1, _exp)
	end
	-- ▲ ReadonlyArray.map ▲
	return _newValue
end):build()
return function(registry)
	registry:registerType(teamType)
end
