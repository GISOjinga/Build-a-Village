import { Signal } from "@rbxts/beacon";
import { SharedConfig } from "../types";
import { ReadonlyDeep } from "../util/data";
import { CenturionLogger } from "../util/log";
import { CommandContext } from "./context";
import { RegistryPath } from "./path";
import { BaseRegistry } from "./registry";
export declare abstract class BaseDispatcher<C extends ReadonlyDeep<SharedConfig> = ReadonlyDeep<SharedConfig>, R extends BaseRegistry<C> = BaseRegistry<C>> {
    protected readonly config: C;
    protected readonly registry: R;
    protected logger: CenturionLogger;
    readonly commandExecuted: Signal<[context: CommandContext<unknown>]>;
    constructor(config: C, registry: R);
    protected executeCommand(executor: Player, path: RegistryPath, inputText: string, args?: string[]): Promise<CommandContext<unknown>>;
}
