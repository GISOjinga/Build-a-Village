-- Compiled with roblox-ts v3.0.0
local TS = _G[script]
local _metadata = TS.import(script, script.Parent, "metadata")
local DecoratorMetadata = _metadata.DecoratorMetadata
local MetadataKey = _metadata.MetadataKey
local function Register(options)
	return function(target)
		local _exp = target
		local _exp_1 = MetadataKey.Register
		local _object = {
			groups = {},
		}
		if options then
			for _k, _v in options do
				_object[_k] = _v
			end
		end
		DecoratorMetadata.defineMetadata(_exp, _exp_1, _object)
	end
end
local function Command(options)
	return function(target, key)
		local _exp = target
		local _exp_1 = MetadataKey.Command
		local _object = {
			name = key,
		}
		if options then
			for _k, _v in options do
				_object[_k] = _v
			end
		end
		DecoratorMetadata.defineMetadata(_exp, _exp_1, _object, key)
	end
end
local function Group(...)
	local groups = { ... }
	return function(target, key)
		DecoratorMetadata.defineMetadata(target, MetadataKey.Group, groups, key)
	end
end
local function Guard(...)
	local guards = { ... }
	return function(target, key)
		DecoratorMetadata.defineMetadata(target, MetadataKey.Guard, guards, key)
	end
end
return {
	Register = Register,
	Command = Command,
	Group = Group,
	Guard = Guard,
}
