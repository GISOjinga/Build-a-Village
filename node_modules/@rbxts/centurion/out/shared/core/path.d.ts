/**
 * A class representing a path to a command or group.
 */
export declare class RegistryPath {
    protected readonly pathParts: string[];
    private pathString;
    constructor(pathParts: string[]);
    static fromString(path: string): RegistryPath;
    static empty(): RegistryPath;
    /**
     * Returns a copy of the path's parts.
     *
     * @returns A copy of the path's parts
     */
    parts(): string[];
    /**
     * Returns the part of this path at the given index.
     *
     * @param index The index of the part
     * @returns The part at the given index
     * @throws If the index is out of bounds
     */
    part(index: number): string;
    /**
     * Gets the root (first element) of this path
     *
     * @returns The root of this path
     */
    root(): string;
    /**
     * Gets the tail (last element) of this path
     *
     * @returns The tail of this path
     */
    tail(): string;
    /**
     * Gets the size of the path
     *
     * @returns The size of the path
     */
    size(): number;
    /**
     * Gets the parent path of this path.
     *
     * If the path is a command, this returns an empty path.
     *
     * @returns The parent path
     */
    parent(): RegistryPath;
    /**
     * Tests whether this path is the child of the given path.
     *
     * @param other Path to test against
     * @returns True if this path is the child of the given path, false if not
     */
    isChildOf(other: RegistryPath): boolean;
    /**
     * Tests whether this path is the descendant of the given path.
     *
     * @param other Path to test against
     * @returns True if this path is the descendant of the given path, false if not
     */
    isDescendantOf(other: RegistryPath): boolean;
    /**
     * Returns a new path from a slice of this path.
     *
     * @param from The start index
     * @param to The end index
     * @returns A new path containing a slice of this path
     */
    slice(from: number, to?: number): RegistryPath;
    /**
     * Determines if this path equals the given path.
     *
     * @param other The path to compare against
     * @returns True if the paths are equal, false if not
     */
    equals(other: RegistryPath): boolean;
    /**
     * Returns the string representation of this path.
     *
     * @returns The string representation of the path
     */
    toString(): string;
    /**
     * Adds the given parts to the path.
     *
     * @param parts The parts to append
     */
    append(...parts: string[]): void;
    /**
     * Removes the part at the given index
     *
     * @param index The index of the part to remove
     * @throws If the index is out of bounds
     */
    remove(index: number): void;
    /**
     * Clears all parts from the path, leaving an empty path
     */
    clear(): void;
    isEmpty(): boolean;
    iter(): Generator<string, void, unknown>;
    private updatePathString;
}
/**
 * An immutable version of a {@link RegistryPath}.
 *
 * Operations that would mutate the path such as `append` or `remove` now
 * return a new {@link ImmutableRegistryPath} with the changes made.
 */
export declare class ImmutableRegistryPath extends RegistryPath {
    /**
     * Creates a {@link ImmutableRegistryPath} from a given {@link RegistryPath}.
     *
     * @param path The {@link RegistryPath}
     * @returns A new {@link ImmutableRegistryPath}
     */
    static fromPath(path: RegistryPath): ImmutableRegistryPath;
    static fromString(path: string): ImmutableRegistryPath;
    static empty(): ImmutableRegistryPath;
    parent(): ImmutableRegistryPath;
    slice(from: number, to?: number): ImmutableRegistryPath;
    /**
     * Returns a new path with the given parts appended.
     *
     * @param parts The parts to append
     * @returns A new path with the given parts appended
     */
    append(...parts: string[]): ImmutableRegistryPath;
    /**
     * Returns a new path with the part at the given index
     * removed.
     *
     * @param index The index of the part to remove
     * @returns A path with the part removed
     * @throws If the index is out of bounds
     */
    remove(index: number): ImmutableRegistryPath;
    /**
     * Creates a new empty {@link ImmutableRegistryPath}.
     *
     * @returns An empty path
     */
    clear(): ImmutableRegistryPath;
}
