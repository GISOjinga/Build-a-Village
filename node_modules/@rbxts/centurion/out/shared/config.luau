-- Compiled with roblox-ts v3.0.0
local TS = _G[script]
local CenturionLogLevel = TS.import(script, script.Parent, "util", "log").CenturionLogLevel
local DEFAULT_CONFIG = {
	registerBuiltInTypes = true,
	logLevel = CenturionLogLevel.Warn,
	guards = {},
	messages = {
		default = "Command executed.",
		error = "An error occurred.",
		notFound = "Command not found.",
	},
	construct = function(ctor)
		local obj = setmetatable({}, ctor)
		local result = obj:constructor()
		local _arg0 = result == nil or result == obj
		assert(_arg0, "Constructors are not allowed to return values.")
		return obj
	end,
}
return {
	DEFAULT_CONFIG = DEFAULT_CONFIG,
}
