-- Compiled with roblox-ts v3.0.0
local TS = _G[script]
local RunService = TS.import(script, TS.getModule(script, "@rbxts", "services")).RunService
local function getRemote(className, name)
	if RunService:IsServer() then
		local _container = script.Parent
		if _container ~= nil then
			_container = _container:FindFirstChild("remotes")
		end
		local container = _container
		if container == nil then
			container = Instance.new("Folder")
			container.Name = "remotes"
			container.Parent = script.Parent
		end
		local remote = Instance.new(className)
		remote.Name = name
		remote.Parent = container
		return remote
	end
	local _instance = script.Parent
	if _instance ~= nil then
		_instance = _instance:WaitForChild("remotes")
	end
	local instance = _instance
	local _arg0 = instance ~= nil and instance.ClassName == "Folder"
	assert(_arg0, "Remotes folder not found")
	local container = instance
	local remote = container:WaitForChild(name)
	local _className = className
	local _arg0_1 = remote.ClassName == _className
	local _arg1 = `Remote '{name}' not found`
	assert(_arg0_1, _arg1)
	return remote
end
local function getRemotes()
	return {
		syncStart = getRemote("RemoteEvent", "SyncStart"),
		syncDispatch = getRemote("RemoteEvent", "SyncDispatch"),
		execute = getRemote("RemoteFunction", "Execute"),
	}
end
return {
	getRemotes = getRemotes,
}
