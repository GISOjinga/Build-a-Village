import { CommandContext, CommandOptions, ImmutableRegistryPath } from "../shared";
import { BaseCommand } from "../shared/core/command";
import { BaseRegistry } from "../shared/core/registry";
import { ReadonlyDeep } from "../shared/util/data";
import { ClientConfig } from "./types";
export declare class ServerCommand extends BaseCommand {
    private readonly config;
    private readonly remote;
    constructor(config: ReadonlyDeep<ClientConfig>, registry: BaseRegistry, path: ImmutableRegistryPath, options: CommandOptions);
    execute(context: CommandContext, args: string[]): void;
    toString(): string;
}
