import { Signal } from "@rbxts/beacon";
import { BaseRegistry } from "../shared/core/registry";
import { SyncData } from "../shared/network";
import { ReadonlyDeep } from "../shared/util/data";
import { ClientConfig } from "./types";
export declare class ClientRegistry extends BaseRegistry<ReadonlyDeep<ClientConfig>> {
    private initialSyncReceived;
    private syncedCommands;
    private syncedGroups;
    readonly synced: Signal<[synced: import("../shared/util/data").ReadonlyDeepObject<SyncData>, incoming: import("../shared/util/data").ReadonlyDeepObject<SyncData>]>;
    /**
     * Initializes the client registry.
     *
     * @internal
     * @ignore
     */
    init(): void;
    /**
     * Begins registry synchronisation to the server.
     *
     * @internal
     * @ignore
     * @throws Throws an error if the server does not respond in time
     * @returns A promise that will be resolved when the initial sync is received
     */
    sync(): Promise<unknown>;
    private registerServerCommand;
}
