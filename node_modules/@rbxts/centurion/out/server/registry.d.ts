import { RegistryPath } from "../shared";
import { BaseCommand, CommandGroup } from "../shared/core/command";
import { BaseRegistry } from "../shared/core/registry";
import { ReadonlyDeep } from "../shared/util/data";
import { ServerConfig } from "./types";
export declare class ServerRegistry extends BaseRegistry<ReadonlyDeep<ServerConfig>> {
    private syncedCommands;
    private initialized;
    /**
     * Initializes the server registry.
     *
     * @param options Server options
     * @internal
     * @ignore
     */
    init(): void;
    isCommandSynced(player: Player, path: RegistryPath): boolean;
    sync(player: Player): void;
    protected addCommand(command: BaseCommand, group?: CommandGroup | undefined): void;
}
